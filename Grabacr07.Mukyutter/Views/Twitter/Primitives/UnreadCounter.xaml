<UserControl x:Class="Grabacr07.Mukyutter.Views.Twitter.Primitives.UnreadCounter"
			 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
			 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
			 xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
			 mc:Ignorable="d"
			 Width="22"
			 Height="22"
			 SnapsToDevicePixels="True"
			 TextOptions.TextFormattingMode="Display">
	<Grid>
		<Border CornerRadius="100"
				Background="{DynamicResource AccentHighlightBrushKey}"
				BorderThickness="0"
				BorderBrush="{DynamicResource AccentHighlightBrushKey}">
		</Border>
		<TextBlock x:Name="count"
				   x:FieldModifier="private"
				   Text="1"
				   FontSize="11"
				   FontFamily="Segoe UI"
				   Foreground="{DynamicResource AccentForegroundBrushKey}"
				   VerticalAlignment="Center"
				   HorizontalAlignment="Center" />
	</Grid>
</UserControl>
